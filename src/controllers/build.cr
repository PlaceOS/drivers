
class Build < Application
  # list the available builds / built files
  def index

  end

  # grab the list of available versions of file / which are built
  def show

  end

  # build a drvier, optionally based on the version specified
  def create

  end

  # delete a built driver
  def delete

  end
end
